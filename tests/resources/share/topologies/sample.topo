

defaults {
  id ```
       "urn:%s:%s" % (component.component_type.lower(), component.properties.name)
  ```
  environment Prod
  layer Machines
  domain StaticTopology
  identifiers [ ``` component.uid ``` ]
  labels [ "staticdemo"]
  data {
    myprop myvalue,
    myarr [ test ]
  }

}

components {
  host (
    name test
    labels ["static:test"]
    relations [ test2, "test3|hosted by"]
  )
  host(name test2, health CRITICAL)
  host(name test3)
}